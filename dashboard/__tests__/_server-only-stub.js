// Empty stub for the `server-only` package used during tests.
// See vitest.config.ts for why this is needed.

// Marker export keeps the module non-empty; consumers only need the
// import to resolve (the real package guard lives in the Next build).
export const SERVER_ONLY_STUB = true;

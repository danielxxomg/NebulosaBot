# Pterodactyl runtime dependencies.
# Keep this file direct-only: pip resolves compatible transitive packages.
# Source of truth: pyproject.toml + uv.lock.
discord.py==2.7.1
supabase==2.31.0
python-dotenv==1.2.2
Pillow==12.2.0
